# ``SwiftBeanCountParserUtils``

Utility methods for parsing Beancount strings outside of ledger files.

## Overview

These are utility methods for parsing data (mostly strings) into SwiftBeanCountModels, which are useful for other SwiftBeanCount projects which need to parse certain data outside of a ledger file, e.g. Importer or Downloader.
